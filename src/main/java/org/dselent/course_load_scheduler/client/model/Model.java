package org.dselent.course_load_scheduler.client.model;

/**
 * Copy / paste from server
 * Need to think a way to prevent code duplication (if possible)
 * The server models have database related data
 * Perhaps a model package where client+server extend from a base set?
 * 
 */
public abstract class Model
{
	
}
