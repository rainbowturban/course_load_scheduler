package org.dselent.course_load_scheduler.client.receive.jsonkeys;

public enum ReceiveRemoveScheduleKeys {
	// not sure if this is what we need
	ID,
	FACULTY_ID,
	SECTIONS_ID,
	//CREATED_AT,
	//UPDATED_AT,
	DELETED
}
